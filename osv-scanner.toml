# OSV-Scanner config: vulnerabilities ignored by ID.
#
# Empty allowlist — no known false positives as of GM-13 (next@16.3.1 upgrade
# cleared the postcss/sharp advisories that were transitive via next@15).
# If OSV reports a false positive, document it here (and in
# security/audit-allowlist.json for the npm gate) instead of weakening
# fail-on-vuln in ci.yml.
