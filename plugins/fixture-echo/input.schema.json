{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["message"],
  "properties": {
    "message": {
      "title": "Message",
      "type": "string",
      "minLength": 1,
      "maxLength": 500,
      "description": "Text echoed into the fixture outputs."
    },
    "tag": {
      "title": "Tag",
      "type": "string",
      "enum": ["fixture", "smoke", "demo"],
      "default": "fixture",
      "description": "Category label written beside the echoed message."
    },
    "force_sleep_seconds": {
      "title": "Force sleep",
      "type": "number",
      "minimum": 0,
      "maximum": 120,
      "default": 0,
      "description": "Test hook to stretch runtime for timeout and cancel flows.",
      "x-unit": "s"
    }
  }
}
