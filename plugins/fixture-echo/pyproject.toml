[project]
name = "modumesh-fixture-echo-plugin"
version = "1.0.0"
description = "Non-CAD fixture plugin for ModuMesh MakerLab contract validation"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=75.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
