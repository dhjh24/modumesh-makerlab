[project]
name = "fixture-mesh"
version = "1.0.0"
description = "Fixture plugin that emits packaged STL/GLB assets"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
